# 개인 포트폴리오 대시보드 PRD

## 1. 목표

한국투자증권과 토스증권의 보유자산을 한 화면에서 확인하고, 시장 지표와 비교해 포트폴리오 분산 상태를 판단한다. 주문·매매 기능은 만들지 않는다.

## 2. 핵심 결정

### 배포와 보안

GitHub Pages는 정적 파일을 배포하는 공개 웹사이트다. 따라서 브라우저 코드, Git 저장소, Pages 결과물에 증권사 API 키, 계좌번호, 보유종목 수량·평가액을 넣지 않는다.

공개 GitHub Pages는 시장 지표와 제품 소개만 제공한다. `내 포트폴리오`를 누르면 인증된 개인 API로 연결한다. 개인 API는 Cloudflare Worker와 비밀 저장소로 구성하고, Cloudflare Access에서 허용한 본인 이메일만 읽기 요청을 통과시킨다. 한국투자증권·토스증권 API 키와 계좌 식별자는 Worker 비밀값에만 둔다. GitHub Actions로 잔고를 생성해 Pages에 공개하는 방식은 사용하지 않는다.

잔고 파일의 로컬 가져오기는 API 설정 전에도 사용할 수 있는 보조 기능으로 남긴다. 가져온 데이터는 해당 브라우저의 IndexedDB에만 저장한다.

### 차트

기본 차트는 TradingView Lightweight Charts를 사용한다. Apache 2.0 오픈소스라 개인 프로젝트에 적합하고, 직접 공급한 보유종목·지표 데이터를 같은 기준 수익률로 겹쳐 비교할 수 있다.

TradingView 위젯은 개별 시장 심화 보기용 외부 링크 또는 보조 화면으로만 사용한다. 위젯에는 사용자 보유 데이터를 연결할 수 없다. Advanced Charts는 별도 접근 승인과 자체 데이터 공급이 필요하고 공개 저장소에 배포할 수 없으므로 1차 범위에서 제외한다.

## 3. 사용자와 시나리오

대상 사용자는 본인 한 명이다.

1. 메인 화면을 열어 총자산, 현금·주식·금·채권 비중, 주요 시장 지표를 확인한다.
2. `상세 차트` 버튼으로 선택 자산 또는 지표의 기간별 흐름을 본다.
3. 보유종목과 벤치마크를 선택해 시작일 기준 100으로 환산한 수익률 차트를 비교한다.
4. 잔고 파일을 다시 가져와 보유내역과 자산배분을 갱신한다.

## 4. 기능 요구사항

### 메인 페이지

- 총 평가액, 전일 대비, 기준일·갱신 시각을 표시한다. 개인 수치는 인증 후에만 표시한다.
- 계좌별 카드. 한국투자증권·토스증권별 평가액, 현금, 보유 종목 수를 표시한다.
- 자산배분 도넛 차트. 기본 분류는 현금, 주식, 방어자산(금·채권)이다. 고급 보기에서 금과 채권을 분리한다.
- 실제 비중과 목표 비중을 함께 표시한다. 목표 대비 차이는 %p로 표시한다.
- 보유 비중 상위 5개와 단일 종목 최대 비중을 표시한다.
- 분산 경고. 단일 종목 최대 비중, 단일 자산군 최대 비중, 목표 비중과의 허용 편차를 사용자가 정한다. 매수·매도 권고는 하지 않는다.
- 시장 지표 카드. KOSPI 100, S&P 500, KRX 금현물, 미국채 10년물, 미국채 30년물의 값·등락 또는 변동·기준 시각·출처를 표시한다.
- 각 카드에 `상세 차트` 버튼을 둔다.

### 상세 차트 페이지

- 기간. 1개월, 3개월, 6개월, 1년, 3년, 전체.
- 표시 방식. 가격·지수값과 기준일 100 수익률을 분리한다.
- 비교. 보유종목 다중 선택, 시장 지표 한 개 이상 선택, 통화와 거래일 차이를 안내한다.
- 보유종목 차트는 평균 매입단가와 현재가를 표시한다. 매입 이력·실현손익 분석은 2차 범위다.

### 데이터 가져오기

- CSV 또는 XLSX 파일을 사용자 기기에서 파싱한다. 원본 파일은 업로드하지 않는다.
- 가져오기 전 미리보기, 중복 계좌·종목 병합 결과, 오류 행을 표시한다.
- 종목 코드는 거래소·통화·자산군·수량·평가액·평균 매입단가를 정규화한다.
- 자산군 자동 분류 결과는 사용자가 수정할 수 있다. 금 ETF·채권 ETF처럼 애매한 상품은 처음 한 번 분류를 물어본다.

### 목표 비중과 분산 경고 설정

- `목표 비중 설정` 카드는 현금, 주식, 방어자산의 색상 구분된 100% 누적 막대를 제공한다.
- 막대 경계 드래그는 양쪽 자산군만 서로 교환하므로 합계가 항상 100%다.
- 각 자산군은 숫자 입력과 1%p 단위 증감 버튼도 제공한다. 숫자를 바꾸면 잠기지 않은 다른 자산군에 차이를 비례 배분하고, 적용 전 결과를 즉시 보여 준다.
- `고급 보기`에서 방어자산을 금·채권으로 나눈 네 항목 편집기로 전환한다. 다시 기본 보기로 전환하면 금과 채권을 합산한다.
- 실제 비중 도넛 바깥 고리에 목표 비중을 표시하고, 카드에는 실제·목표·차이 %p를 함께 표시한다.
- 분산 경고 설정은 단일 종목 최대 비중, 자산군 최대 비중, 목표 대비 허용 편차를 숫자 입력 또는 슬라이더로 저장한다. 기본값은 제안만 하고 사용자가 언제든 변경한다.

```text
┌ 실제 비중 ────────┐  ┌ 목표 비중 설정 ───────────────────────┐
│       도넛        │  │ 현금  10%  [====|                     ] │
│ 현금 12%  +2%p    │  │ 주식  60%  [============|             ] │
│ 주식 58%  -2%p    │  │ 방어  30%  [======|                   ] │
│ 방어 30%   0%p    │  │ 합계 100%              고급 보기     │
└───────────────────┘  └───────────────────────────────────────┘
```

## 5. 데이터 원칙

| 항목 | 1차 데이터 기준 | 표시 단위 |
| --- | --- | --- |
| KOSPI 100 | 라이선스가 확인된 KRX 또는 증권사 시세 | 지수·등락률 |
| S&P 500 | 라이선스가 확인된 지수 시세 공급원 | 지수·등락률 |
| KRX 금현물 | KRX 공식 데이터 또는 계약된 시세 공급원 | g당 원화·등락률 |
| 미국채 10년·30년 | 미국 재무부 일별 Par Yield Curve | 수익률 %·전일 대비 bp |
| 계좌·보유종목 | 사용자가 기기에서 가져온 잔고 파일 | 원화 환산 평가액 |

미국채 10년·30년은 가격이 아니라 수익률이다. 미국 재무부 공식 수익률 곡선은 일별 공표치이므로 1분마다 새 값이 생기지 않는다. 매일 최신 공식값을 가져오고 다음 공표 전에는 마지막 공표 시각을 표시한다.

KOSPI 100, S&P 500, KRX 금현물은 계약·이용 범위가 확인된 시세 공급원에서 1분마다 Worker가 갱신한다. 1분 갱신 실패 시 마지막 정상값과 지연 시간을 표시하고, 15분이 지나면 오류 상태로 바꾼다. 화면에 지연·기준 시각·출처를 항상 표시한다.

환율은 KRW 기준 총자산 계산에 필요하다. 1차에서는 잔고 파일의 평가액을 우선 사용하고, 2차 자동 동기화에서 공식·계약된 환율 데이터로 통일한다.

### 데이터 공급원 권장안

계좌·보유내역은 한국투자증권 Open API와 토스증권 Open API를 각각 직접 사용한다. 두 API가 이미 필요한 인증·계좌 데이터를 제공하므로 비공식 스크래핑이나 별도 계좌 통합 서비스는 추가하지 않는다.

지표 시세는 먼저 두 증권사 API의 실제 지원 종목과 약관을 확인한다. 인증된 개인 대시보드에 한해 필요한 다섯 지표를 모두 제공하고 표시가 허용되면, 계좌 API와 같은 공급원을 우선 사용한다. 이것이 운영할 API 키와 장애 지점을 가장 적게 만든다.

공개 GitHub Pages로 1분 시세를 재배포하는 권한은 별도 확인이 필요하다. 해당 약관이 이를 허용하지 않으면 증권사 데이터를 공개 캐시하지 않는다. 공개 화면은 허용된 TradingView 위젯으로 대체하거나, KOSPI 100·S&P 500·KRX 금현물을 모두 포함하고 공개 웹 표시·1분 갱신을 계약으로 보장하는 유료 공급원을 선정한다. 무료 웹 스크래핑은 사용하지 않는다.

## 6. 정보 구조

```text
메인 /
├─ 자산 요약과 도넛 차트
├─ 계좌별 요약
├─ 보유 비중과 분산 경고
├─ 시장 지표 카드
└─ 상세 차트 /charts
   ├─ 지표 차트
   └─ 보유종목·벤치마크 비교

가져오기 /import
└─ 파일 미리보기, 검증, 로컬 저장
```

## 7. 단계별 범위

### 1차. 공개 페이지와 인증 개인 API

- GitHub Pages에 공개 시장 지표 페이지와 차트 페이지를 배포.
- Cloudflare Worker의 1분 Cron으로 시세를 수집하고 캐시한다.
- Cloudflare Access로 개인 API를 본인 이메일만 허용한다.
- 한국투자증권 Open API와 토스증권 Open API는 읽기 전용 계좌·잔고·시세 조회만 허용한다.
- 메인 요약, 실제·목표 자산배분 도넛, 100% 목표 비중 편집기, 지표 카드, 수익률 비교 차트.
- 로컬 CSV/XLSX 가져오기와 IndexedDB 저장은 API 설정 전 보조 수단으로 제공한다.

### 2차. 분석 고도화

- 기간 수익률, 자산군별 기여도, 리밸런싱 목표 비중 대비 편차.
- 현금흐름을 반영한 수익률. 매입·매도 이력 가져오기가 선행 조건이다.
- 월간 스냅샷과 PDF/CSV 내보내기.

## 8. 제외 범위

- 매수, 매도, 주문 변경·취소.
- 투자 추천, 목표 수익률 제시, 자동 리밸런싱 주문.
- Pages·저장소·클라이언트 코드에 API 키나 개인 잔고 데이터 저장.
- 무단 스크래핑 또는 사용권이 불명확한 실시간 지수 데이터.

## 9. 완료 기준

1. 인증된 사용자만 두 증권사 계좌와 통합 총자산을 볼 수 있다.
2. 도넛 합계는 총자산과 일치하고, 분류가 없는 자산은 기타로 명확히 표시된다.
3. 목표 비중 편집기는 항상 100%를 유지하고, 실제 비중과 목표 대비 차이를 표시한다.
4. 사용자가 정한 분산 경고 기준을 초과하면 해당 항목과 초과 %p가 표시된다.
5. 다중 보유종목과 한 개 이상 벤치마크를 기준일 100 차트에서 비교할 수 있다.
6. 다섯 시장 지표에 값, 기준 시각, 출처, 지연 여부가 표시된다.
7. 1분 시세 갱신 실패 시 마지막 정상값·지연 시간·오류 상태가 표시된다.
8. 인증되지 않은 요청은 개인 API에서 잔고를 받지 못한다.
9. 빌드 산출물과 Git 기록에서 API 키·계좌번호·개인 보유내역이 발견되지 않는다.

## 10. 구현 전 결정할 항목

- Cloudflare 계정과 Access에 허용할 본인 이메일을 준비한다.
- 한국투자증권·토스증권 Open API의 읽기 전용 키를 발급하고, 허용 IP·호출 한도를 확인한다.
- KOSPI 100, S&P 500, KRX 금현물의 표시·배포가 허용된 1분 시세 공급원을 계약 또는 사용 약관 검토 후 확정한다.
- 분산 경고 기본값. 단일 종목 20%, 단일 자산군 60%, 목표 대비 허용 편차 5%p를 제안한다. UI에서 변경 가능하다.
- 총자산 평가 통화. 기본 제안은 KRW다.

## 11. 근거

- [GitHub Pages 정적 호스팅](https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages).
- [한국투자증권 Open API 계좌·잔고·시세 문서](https://apiportal.koreainvestment.com/apiservice-summary).
- [토스증권 Open API](https://p.tossinvest.com/ko/open-api).
- [TradingView Lightweight Charts 라이선스와 기능](https://www.tradingview.com/lightweight-charts/).
- [TradingView 제품 비교와 위젯 제약](https://www.tradingview.com/charting-library-docs/latest/product-comparison/).
- [미국 재무부 일별 수익률 곡선](https://home.treasury.gov/resource-center/data-chart-center/interest-rates/TextView?page=1&type=daily_treasury_yield_curve).
- [Cloudflare Workers Cron Trigger](https://developers.cloudflare.com/workers/configuration/cron-triggers/).
- [Cloudflare Access 애플리케이션 보호](https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/).
